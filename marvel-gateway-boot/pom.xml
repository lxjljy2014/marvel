<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.marvel</groupId>
        <artifactId>marvel</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>marvel-gateway-boot</artifactId>
    <name>marvel-gateway-boot</name>
    <description>启动器：聚合各业务模块（未来其聚合职责由 Gateway + 独立部署的服务取代）</description>

    <dependencies>
        <dependency>
            <groupId>com.marvel</groupId>
            <artifactId>module-system</artifactId>
        </dependency>
        <dependency>
            <groupId>com.marvel</groupId>
            <artifactId>module-auth</artifactId>
        </dependency>
        <dependency>
            <groupId>com.marvel</groupId>
            <artifactId>module-infra</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
